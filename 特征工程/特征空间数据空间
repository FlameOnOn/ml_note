这两幅图表示的都是特征空间中的数据向量。坐标轴表示单词，也就是词袋表示中的特
征，空间中的点表示数据点（文本文档）。有时候，在数据空间中查看特征向量也能获取
很多信息。在数据空间中，特征向量中包含的是每个数据点中各个特征的值。坐标轴表示
各个数据点，其中的点表示特征向量。图 3-5 给出了一个这样的例子。通过对文本文档的
词袋特征化，一个特征就是一个单词，一个特征向量由这个单词在每篇文档中出现的次数
组成。这样，一个单词就可以表示为“文档袋”。正如我们将在第 4 章中看到的，这些文
档袋向量来自于词袋向量的矩阵转置。
